import styled from "styled-components";

export default styled.div`
  padding: 4rem 0 5rem 0;

  .section-heading {
    text-align: center;
    font-size: 3.2rem;
  }

  .list-container {
    margin-top: 4rem;
  }
`;