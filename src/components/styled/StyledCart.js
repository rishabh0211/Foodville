import styled from "styled-components";

export default styled.div`
  .heading {
    font-size: 3.2rem;
    font-weight: 600;
  }

  .item-count {
    margin-top: .4rem;
    text-transform: uppercase;
  }

  .items-list {
    margin-top: 4rem;
  }
`;