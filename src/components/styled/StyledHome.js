import styled from "styled-components";

export default styled.main`
  .main-section {
    min-height: calc(100vh - 5.9rem);
    width: 100%;
    max-width: 104rem;
    margin: auto;
    padding: 0 2rem;
  }
`;